# DeepListener 项目待办事项 (TODO)

## 优先级：高 (Core Features)
- [ ] **完善 SRS 算法**：从当前的简易间隔法升级为更精准的 SM-2 或类似 Anki 的算法。
- [ ] **支持更多音视频格式**：增加对 `.m4a`, `.mp4` 提取音频的支持。
- [ ] **批量上传**：支持一次性上传多段音频并进入转录队列。

## 优先级：中 (Experience)
- [ ] **快捷键优化**：在精听台增加更多单手操作快捷键（例如：`J` 后退 3s，`K` 播放/暂停，`L` 前进 3s）。
- [ ] **波形渲染性能**：针对长音频，优化 WaveSurfer 的渲染速度。

## 优先级：低 (Architecture & Infrastructure)
- [ ] **迁移至 Supabase**：将本地 SQLite 迁移到云端，实现真正的全平台同步。
- [ ] **PWA 安装优化 (重点记录)**：
    - **问题**：在 Android 局域网 http 环境下，PWA 图标无法出现在桌面，即使显示“已安装”也搜不到图标。
    - **现状**：已配置 `manifest.json`, `sw.js`, 且已生成合法的 192/512 PNG 图标。
    - **可能原因**：非 HTTPS 带来的 WebAPK 生成失败，或国产安卓系统对快捷方式的拦截。
    - **后续对策**：尝试部署到 Vercel (HTTPS)，或者使用内网穿透工具（如 Localtunnel/Ngrok）提供 HTTPS 测试环境。

---
*记录时间：2026-01-24*
