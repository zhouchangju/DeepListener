# Decision Log

Durable decisions that future agents should inherit.

| ID | Decision | Reason | Owner | Date | Impact | Related Items |
|---|---|---|---|---|---|---|
| DEC-001 | Use SQLite for local database | Simple, portable, sufficient for single-user app | Project owner | 2026-01 | All data operations use Prisma with SQLite | prisma/schema.prisma |
| DEC-002 | Use undici ProxyAgent for proxy | Node.js 18+ fetch ignores HTTP_PROXY; needed for OpenAI/Google in restricted networks | Project owner | 2026-01 | Transcription providers require proxy config | src/lib/transcription/ |
| DEC-003 | Server Components + Client Hydration | Data fetching on server, interactivity on client | Project owner | 2026-01 | All routes use RSC for data, client components for UI | src/app/ |
| DEC-004 | Factory pattern for transcription | Multi-provider support with env-based selection | Project owner | 2026-01 | Adding new providers requires factory update | src/lib/transcription/factory.ts |
| DEC-005 | FSRS-4.5 for spaced repetition | State-of-the-art SRS algorithm with stability/difficulty | Project owner | 2026-02 | Review system depends on FSRS parameters | src/lib/fsrs.ts |
| DEC-006 | Zero-delay shadowing via AudioBuffer | Pre-decode entire audio, slice in memory for immersion | Project owner | 2026-02 | ShadowingConsole requires full audio decode on load | src/components/feature/ShadowingConsole.tsx |
| DEC-007 | Colocated tests alongside source | Co-locate *.test.ts with implementation for discoverability | Project owner | 2026-05 | All test files live next to source files | src/**/*.test.ts |
| DEC-008 | Agent harness with Contract/Adversarial modes | Protect production data while enabling safe iteration | Project owner | 2026-06 | All non-trivial changes require harness workflow | docs/agent-harness/ |
| DEC-009 | Desktop database boot uses packaged migrations and fails closed | A renderer must never open against a partially migrated or incompatible database | AI Agent | 2026-08-02 | Desktop passes an explicit migration directory; initialization failure terminates the embedded service | desktop/main.js, src/instrumentation.ts, src/lib/migration-runner.ts |
| DEC-010 | Packaged macOS Desktop stores provider credentials in one Keychain item | API keys should not remain in a plaintext settings file on the desktop path; Server/dev keeps the deterministic file backend | AI Agent | 2026-08-02 | `DEEPLISTENER_SECRET_BACKEND=keychain` is injected only for macOS Electron; legacy files are removed after a successful Keychain write | src/lib/secrets-store.ts, desktop/main.js |
| DEC-011 | Public desktop packaging fails closed on missing redistributable media/demo assets | An unsigned internal alpha may use system FFmpeg and synthetic demo data, but a public build must not silently ship those dependencies | AI Agent | 2026-08-02 | `desktop:preflight` blocks the default path; `desktop:dist --dir --alpha` is the explicit internal escape hatch | scripts/desktop-preflight.mjs, scripts/desktop-dist.mjs |
| DEC-012 | Use a native anchor for the Setup Provider hash deep link | Next.js client navigation updates the URL with `pushState` and does not emit `hashchange`, so a same-page decision-card click would otherwise require a second Configure action | AI Agent | 2026-08-05 | `/setup#provider-settings` opens the real Provider dialog in one learner action while preserving direct-link behavior | src/app/setup/TranscriptionDecisionGuide.tsx, src/app/setup/ProviderCardActions.tsx |
| DEC-013 | Explain subtitle import's two mutually exclusive paths in the dialog copy | Ordinary learners need to know that a local SRT/VTT removes the Provider requirement, while media-only import still depends on the configured Provider | AI Agent | 2026-08-05 | Subtitle import copy now states the condition before file selection in both locales | messages/en.json, messages/zh-CN.json |
| DEC-014 | Show short visible labels for sentence-level actions at large widths while keeping compact narrow-screen controls | Icon-only controls were discoverable to assistive technology but not obvious to a novice learner; labels reuse existing localized keys and do not change action semantics | AI Agent | 2026-08-05 | Practice/Demo sentence cards expose Shadowing, Copy, and Save labels on large screens; narrow layouts remain compact | src/components/feature/audio-player/SentenceList.tsx |
| DEC-015 | Recovery UI may offer only providers already configured on the local profile, and must expose elapsed retry state | A novice user should not be sent into a provider choice that is guaranteed to fail; silent or indefinite retries feel like a hang and increase duplicate submissions | AI Agent | 2026-08-05 | Library passes a masked configured-provider list; receiving operations lock provider selection; retries announce elapsed seconds through a localized live region | src/app/library/page.tsx, src/app/library/ImportRecoveryList.tsx, src/app/library/UploadButton.tsx, src/app/library/BatchUploadButton.tsx |
| DEC-016 | Readiness checks the nearest existing writable ancestor for not-yet-created Desktop media directories | A clean Desktop data root legitimately starts without `media/audio` and `media/video`; treating missing child directories as blockers creates a false manual setup task even though import owns directory creation | AI Agent | 2026-08-05 | Read-only readiness reports fresh media paths as ready when their ancestor is writable, while preserving action status for existing read-only paths | src/lib/setup-readiness.ts, src/lib/setup-readiness.test.ts |
| DEC-017 | Keep retry elapsed seconds visual-only and announce retry state once | Updating a `role=status` string every second can overwhelm screen-reader users; the learner still needs visible progress without repeated speech | AI Agent | 2026-08-05 | Recovery renders a hidden live status for the retry start and an `aria-hidden` elapsed timer for sighted users in both locales | src/app/library/ImportRecoveryList.tsx, messages/en.json, messages/zh-CN.json |
| DEC-018 | Match the file-picker label to the import mode | A single-file picker labelled as a folder action makes the first import step ambiguous for ordinary learners; batch mode is the only flow that selects multiple files from a folder | AI Agent | 2026-08-05 | Single import says “Choose a file” / “选择文件”; batch import keeps the folder wording without changing the input contract | src/app/library/UploadDropDialog.tsx, messages/en.json, messages/zh-CN.json |
| DEC-019 | Warn before a provider-dependent subtitle-wizard import when no provider is configured | Uploading a large audio file only to discover that transcription cannot start is an avoidable novice dead end; the warning must preserve the valid embedded-subtitle video path | AI Agent | 2026-08-05 | The wizard explains SRT/VTT and embedded-subtitle alternatives and links directly to Provider setup without disabling import or exposing secrets | src/app/library/ImportMediaWizard.tsx, src/app/library/UploadButton.tsx, messages/en.json, messages/zh-CN.json |
| DEC-020 | Localize the seeded Demo title in the Practice surface | A Chinese-speaking first-time learner should not encounter an English-only Demo title after the landing CTA succeeds; the stored seed title remains stable for data and export compatibility | AI Agent | 2026-08-05 | Demo Practice renders the locale-specific `practice.demoTrackTitle` while normal tracks keep their stored titles | src/app/practice/[id]/PracticeClient.tsx, messages/en.json, messages/zh-CN.json |
| DEC-021 | Use service-key wording on no-key first-session paths, while retaining API-key wording inside the credential form | “API key” is an implementation term that can make a novice think setup is mandatory before a local subtitle or offline Demo; the Provider form must still name the exact credential being pasted | AI Agent | 2026-08-05 | Landing, Demo, decision-guide, subtitle-import, and recovery copy now say service key / 服务密钥; the explicit Provider dialog remains precise | messages/en.json, messages/zh-CN.json, src/app/landing-translations.ts, src/i18n/first-session-language.test.ts |
| DEC-022 | Execute public Desktop preflight against a disposable target in the Demo release contract test | A source-only assertion can drift from runtime behavior; the public gate must be proven to reject the current synthetic Demo and missing target-specific FFmpeg assets without touching real assets | AI Agent | 2026-08-05 | `desktop-packaging-contract.test.ts` runs the read-only preflight with a fake target and asserts explicit failure diagnostics; alpha flags remain an explicit escape hatch | src/lib/desktop-packaging-contract.test.ts, scripts/desktop-preflight.mjs |
| DEC-023 | Do not imply that an already-configured Provider can be saved with a blank key | The dialog save contract requires a new credential; a “leave blank to keep” hint would create a dead-end for ordinary learners and contradict the actual button behavior | AI Agent | 2026-08-05 | Configured state is shown as status only, and the form explicitly says that entering a new key replaces it; the key remains write-only | src/app/setup/ProviderConfigDialog.tsx, src/app/setup/ProviderConfigDialog.structure.test.ts |
| DEC-024 | Block guaranteed provider-dependent audio starts when no Provider or sidecar subtitle exists | Uploading an audio file before setup wastes time and postpones an inevitable failure; video must remain available because embedded subtitles can satisfy the local path | AI Agent | 2026-08-05 | Subtitle wizard disables only the no-subtitle audio case, keeps embedded-video imports available, and exposes bilingual recovery guidance | src/app/library/ImportMediaWizard.tsx, messages/en.json, messages/zh-CN.json, src/app/library/ImportMediaWizard.test.ts |
| DEC-025 | Validate subtitle timing against media duration with advisory and blocking levels | A novice should get an actionable mismatch warning without having a valid file rejected solely because a container reports a rounded duration; malformed or materially out-of-range cues must be blocked before persistence | AI Agent | 2026-08-05 | Shared pure validation checks cue shape, overlap, duration availability, and a 2-second/10-percent tolerance; callers keep a stable safe reason contract | src/lib/subtitle-media-validation.ts, src/lib/subtitle-utils.ts, src/lib/subtitle-media-validation.test.ts |
| DEC-026 | Expose Demo removal only after the server confirms Demo ownership exists | Learners need a clear cleanup path after trying the Demo, but the UI must never imply that arbitrary personal data can be removed; the existing API already scopes deletion to `trackType = DEMO` | AI Agent | 2026-08-05 | Setup reads `GET /api/demo`, asks for explicit confirmation, then calls `DELETE /api/demo`; no client-side data deletion or secret/path exposure is added | src/app/setup/DataSafetyActions.tsx, src/app/api/demo/route.ts, src/lib/demo-seed.ts |
| DEC-027 | Show conditional no-provider guidance beside the generic Library import action | The generic import button was truthful about file types but did not tell a learner without a service that audio without subtitles cannot be transcribed; a conditional hint preserves the flexible upload path while exposing real subtitle and setup destinations | AI Agent | 2026-08-05 | Library only shows the warning when the server supplies an empty configured-provider list; the copy and links are localized, and an omitted list remains an unknown state | src/app/library/UploadButton.tsx, messages/en.json, messages/zh-CN.json, src/app/library/UploadButton.test.ts |
| DEC-028 | Mark Deepgram as the single default Provider starting recommendation in Setup | Ordinary learners need a visible starting point when several Providers are available, but the product must not imply live regional availability, pricing, or connectivity; the marker therefore remains static and explicitly leaves HG-02 approval open | AI Agent | 2026-08-05 | `ProviderGuidance.recommended` drives one localized badge and reason; OpenAI/Google remain available; Setup load performs no connectivity test | src/lib/provider-guidance.ts, src/app/setup/TranscriptionDecisionGuide.tsx, messages/en.json, messages/zh-CN.json |
| DEC-029 | Describe embedded subtitles as a video path and route audio learners to SRT/VTT | The implementation can inspect embedded captions on video, while audio-only imports cannot satisfy the no-Provider path without a sidecar subtitle; wording that says “audio or video with embedded subtitles” creates a deterministic novice dead end | AI Agent | 2026-08-05 | Decision-guide copy now says video with embedded subtitles; audio guidance points to SRT/VTT without changing import behavior | messages/en.json, messages/zh-CN.json, src/i18n/first-session-language.test.ts |
| DEC-030 | Block no-provider audio at every generic import entrypoint before upload | Blocking only the subtitle wizard left the generic single and batch import buttons able to upload audio that was guaranteed to fail at transcription; fail-fast preserves media and gives the learner an executable recovery path | AI Agent | 2026-08-05 | Single and batch imports use client media classification to stop audio when the server reports no configured Provider; video and configured/unknown profiles retain existing behavior | src/app/library/UploadButton.tsx, src/app/library/BatchUploadButton.tsx, messages/en.json, messages/zh-CN.json |
| DEC-031 | Validate SRT/VTT content locally before creating an import job | Extension-only validation still allowed empty or malformed subtitle files to upload a large media source before failing; the client can reuse the pure parser while the server remains authoritative | AI Agent | 2026-08-05 | ImportMediaWizard parses and validates the selected sidecar before `POST /api/import-jobs`; invalid subtitles fail fast without touching media or Provider boundaries | src/app/library/ImportMediaWizard.tsx, src/lib/subtitle-utils.ts, src/lib/subtitle-media-validation.ts |
